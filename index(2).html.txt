<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Game Mechanics Constructor</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Manrope:wght@400;600;800&display=swap');

        :root {
            --primary: #4F46E5;
            --accent: #EC4899;
            --dark: #111827;
            --light: #F9FAFB;
        }

        body {
            font-family: 'Manrope', sans-serif;
            background: linear-gradient(135deg, #E0E7FF 0%, #F3E8FF 100%);
            margin: 0;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 20px;
            color: var(--dark);
        }

        .glass-card {
            width: 100%;
            max-width: 400px;
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-radius: 32px;
            border: 1px solid rgba(255, 255, 255, 0.6);
            box-shadow: 0 25px 50px -12px rgba(79, 70, 229, 0.15);
            padding: 40px 30px;
            position: relative;
            overflow: hidden;
        }

        h2 {
            font-size: 26px;
            font-weight: 800;
            margin: 0 0 12px;
            background: linear-gradient(to right, var(--primary), var(--accent));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.5px;
        }

        p {
            font-size: 16px;
            color: #6B7280;
            margin-bottom: 32px;
            line-height: 1.6;
        }

        .step { display: none; }
        .step.active { display: block; animation: slideIn 0.5s cubic-bezier(0.16, 1, 0.3, 1); }

        .option-btn {
            width: 100%;
            padding: 18px 20px;
            margin-bottom: 14px;
            background: #FFFFFF;
            border: 1px solid #E5E7EB;
            border-radius: 16px;
            font-size: 16px;
            font-weight: 600;
            color: var(--dark);
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 12px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.02);
        }

        .option-btn:hover {
            transform: translateY(-3px);
            border-color: var(--primary);
            box-shadow: 0 10px 25px rgba(79, 70, 229, 0.1);
            color: var(--primary);
        }

        .result-box {
            background: var(--dark);
            color: white;
            padding: 30px;
            border-radius: 24px;
            margin-top: 20px;
            position: relative;
            z-index: 2;
        }

        .res-title {
            font-size: 20px;
            font-weight: 800;
            margin-bottom: 12px;
            color: #A5B4FC;
        }

        .res-desc {
            font-size: 15px;
            opacity: 0.9;
            line-height: 1.6;
            margin-bottom: 24px;
        }

        .cta-btn {
            background: linear-gradient(90deg, var(--primary), var(--accent));
            color: white;
            padding: 16px;
            border-radius: 14px;
            text-decoration: none;
            font-weight: 700;
            display: block;
            text-align: center;
            box-shadow: 0 10px 20px rgba(236, 72, 153, 0.3);
            transition: transform 0.2s;
        }

        .cta-btn:hover { transform: scale(1.02); }

        @keyframes slideIn {
            from { opacity: 0; transform: translateX(20px); }
            to { opacity: 1; transform: translateX(0); }
        }
    </style>
</head>
<body>

<div class="glass-card">
    <!-- Шаг 1 -->
    <div class="step active" id="s1">
        <h2>Что вы продаете?</h2>
        <p>Выберите сферу, чтобы алгоритм подобрал механику.</p>
        <button class="option-btn" onclick="next(1, 'uslugi')">💼 Услуги / Наставничество</button>
        <button class="option-btn" onclick="next(1, 'tovary')">🧶 Товары / Хендмейд</button>
        <button class="option-btn" onclick="next(1, 'obuchenie')">🎓 Обучение / Курсы</button>
    </div>

    <!-- Шаг 2 -->
    <div class="step" id="s2">
        <h2>Главная цель запуска</h2>
        <p>Какой результат нужен в первую очередь?</p>
        <button class="option-btn" onclick="next(2, 'prodazhi')">💰 Быстрые продажи</button>
        <button class="option-btn" onclick="next(2, 'progrev')">🤝 Прогрев и доверие</button>
        <button class="option-btn" onclick="next(2, 'aktiv')">🚀 Активация базы</button>
    </div>

    <!-- Шаг 3 -->
    <div class="step" id="s3">
        <h2>Ресурс на ведение</h2>
        <p>Сколько времени готовы уделять процессу?</p>
        <button class="option-btn" onclick="finish('min')">⏱ Минимум (только бот)</button>
        <button class="option-btn" onclick="finish('mid')">📝 Бот + контент</button>
        <button class="option-btn" onclick="finish('max')">🔥 Полный движ</button>
    </div>

    <!-- Результат -->
    <div class="step" id="res">
        <h2>Ваша механика:</h2>
        <div class="result-box">
            <div class="res-title" id="rT">...</div>
            <div class="res-desc" id="rD">...</div>
            <a href="#" class="cta-btn">Забрать шаблон в клубе</a>
        </div>
    </div>
</div>

<script>
    let d = {};
    function next(s, v) { 
        d[s]=v; 
        document.getElementById('s'+s).classList.remove('active'); 
        document.getElementById('s'+(s+1)).classList.add('active'); 
    }
    function finish(v) {
        d['t']=v;
        document.getElementById('s3').classList.remove('active');
        document.getElementById('res').classList.add('active');
        
        let t = "", x = "";
        if(d[1]=='uslugi' && d[2]=='prodazhi') { t="⚡️ Блиц-аукцион"; x="Создайте дефицит мест через бота. Кто первый нажал кнопку — того и место. Идеально для услуг."; } 
        else if(d[1]=='tovary') { t="📦 Тайный ящик"; x="Люди обожают распаковки. Спрячьте товар за интригующими карточками в боте."; } 
        else if(d[2]=='progrev') { t="🕵️‍♀️ Детективное расследование"; x="Выдавайте пользу частями как 'улики'. В финале — ссылка на продукт как разгадка."; } 
        else { t="🗺️ Карта сокровищ"; x="Универсальная механика. Собираем кодовое слово из постов для получения бонуса."; }
        
        document.getElementById('rT').innerText = t;
        document.getElementById('rD').innerText = x;
    }
</script>

</body>
</html>